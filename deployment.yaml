apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: vue-natuer-remo
  name: vue-natuer-remo
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vue-natuer-remo
  template:
    metadata:
      labels:
        app: vue-natuer-remo
    spec:
      containers:
      - image: docker-registry.k3s.nuc/haku-mai/vue-natuer-remo:develop
        name: vue-natuer-remo
      imagePullSecrets:
      - name: docker-registry-secret
